# Compose-level environment: copy to `.env` and fill in.
# (Service-level variables live in server/.env.example and client/.env.example.)

# REQUIRED: token signing secret. Generate with: openssl rand -base64 48
JWT_SECRET=change-me-to-a-long-random-secret
